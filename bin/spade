#!/usr/bin/env ruby
lib = File.expand_path(File.dirname(__FILE__) + '/../lib')
$:.unshift(lib) if File.exists?(lib)

require 'spade'
require 'spade/cli'

Spade::CLI.run(File.basename(__FILE__), Dir.pwd, ARGV)
