#!/usr/bin/env ruby
lib = File.expand_path(File.dirname(__FILE__) + '/../lib')
$:.unshift(lib) if File.exists?(lib)

require 'tiki'
require 'tiki/cli'

Tiki::CLI.run(File.basename(__FILE__), Dir.pwd, ARGV)
